name: inflection
description: "Plataforma de optimización conductual y neurociencias."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Gestión de Estado
  flutter_riverpod: ^2.5.0

  # Enrutamiento y UI
  go_router: ^14.0.0
  google_fonts: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
